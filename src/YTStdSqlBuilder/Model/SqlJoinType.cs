namespace YTStdSqlBuilder;

/// <summary>SQL JOIN 类型枚举</summary>
public enum SqlJoinType
{
    /// <summary>INNER JOIN</summary>
    Inner,
    /// <summary>LEFT JOIN</summary>
    Left,
    /// <summary>RIGHT JOIN</summary>
    Right
}
